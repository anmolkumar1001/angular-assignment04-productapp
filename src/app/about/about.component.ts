import { Component } from '@angular/core';

@Component({
  selector: 'app-about',
  template: `
    <h2>About Products</h2>
    <p>This app demonstrates obtaining data from a server through an Angular service using HttpClient.</p>
  `
})
export class AboutComponent {}
